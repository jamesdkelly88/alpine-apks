services:
  builder:
    image: alpine:3.23
    container_name: builder
    entrypoint: /bin/sh
    stdin_open: true # docker run -i
    tty: true        # docker run -t
    restart: no
    volumes:
    - ./packages:/packages